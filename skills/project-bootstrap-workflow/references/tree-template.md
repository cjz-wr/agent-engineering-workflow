# tree.md Template

> v2.2 — 从 Base Protocol 第 2.2 节提取，可直接复制使用。

## 目录结构

```
.
├── src/
│   └── ...
├── tests/
│   └── ...
└── ...
```

## 记录约束

MUST NOT 记录以下内容：

- `node_modules/`、`build/`、`dist/`、`.git/` 等依赖与产物目录。
- 临时文件、缓存文件、日志文件。
